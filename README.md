# NUMBER RUSH

## v8.0：探し合う緊張感と、取り勝った手応え

『ワギャンランド2』の数字さがしを目標に、数字を探す対戦の分かりやすさを改善しました。キャラクター・曲・描画はオリジナルです。今回BGMの譜面は変更していません。

- CPUのピンクの探索カーソルが未獲得パネルを移動します。これは演出で、正解の場所を教えたり、CPUの探索期限を変更したりしません。
- 自分のカーソル・獲得パネル・得点ゲージ・盤面外周の獲得履歴は青、CPU側はピンク。数字は隠さない細いカーソルです。
- お題の見出しと `YOU +1 / CPU +1` を大きく表示。正解速度に応じた `LIGHTNING / QUICK / NICE`、回答秒数、連続正解数で手応えを伝えます。速度評価は追加得点ではありません。
- 勝利まであと何点か、勝利ライン到達、逆転ができなくなった場合の自己ベスト挑戦を表示します。勝利ライン到達後も全問まで遊べます。
- 盤面は矢印キーで移動、EnterまたはSpaceで決定。端から反対側へ移れます。従来のクリック・タップも使えます。
- メニューの `HELP` またはHで遊び方。Enter・Esc・BACKでメニューへ戻れます。
- 対戦・練習中のEscで一時停止。ブラウザのタブやアプリを離れた場合も、非表示イベントを受けて時計・CPU・音を停止します。戻ってから `RESUME` またはEnterで2秒カウント後に再開。停止画面の `TITLE` は確認を経てメニューへ戻ります。
- 5×8マス、4種類の数字範囲、ORDER/RANDOM、難易度、ミス時に再選択できる爆発、iPhoneの画面内フィットは維持しています。

ブラウザのゲーム部分を一度クリックするとキーボード操作を受け付けます。Escでアプリ自体を終了しないよう、Pyxel標準の終了キーを無効化しています。デスクトップ版の終了はウィンドウの×を使ってください。

## 対戦専用BGM

CPU対戦はオリジナル曲 **Forest Face-Off** を使用します。ニ短調の短い主題の掛け合い、刻むベースとキックで対決感を出し、後半は音量を上げずにリズムを追加します。32小節・76.8秒のループと、確認画面から戻った際の曲位置保持は維持しています。練習モードのForest Stroll、メニュー・待機・結果曲は変更していません。

## 画面ごとのBGM

- モード選択：穏やかなメニュー曲（25.6秒ループ）。
- RETRY/TITLE確認中：静かな待機曲（8秒ループ）。時計・CPUは停止したままです。
- 開始/復帰カウント：軽い上昇フレーズ（3.2秒ループ、カウント終了で切替）。
- 勝利/練習クリア：明るい結果曲（9.6秒ループ）。
- 敗北：落ち着いた響きから明るく着地する結果曲（12.8秒ループ）。

ゲーム中の曲は変更していません。確認を取り消すと、ゲーム中BGMは中断位置から再開します。結果曲は効果音が鳴り終わってから開始し、BGM OFFはすべての画面に共通です。ブラウザの制限上、最初のクリック/タップ前は無音です。実際の聞き心地は再生環境でご確認ください。

## iPhoneの画面サイズ対応

ゲームを `player.html` の640×360固定の専用iframe内で実行し、`viewport.js` が枠ごと縮小してSafariの可視領域・セーフエリアに16:9で収めます。Pyxel内部の最小倍率による切り取りを防ぎます。縦向きでも全ボタンを表示し、横向きでは説明を隠してゲーム領域を広げます。回転やアドレスバーの伸縮時もゲームを再起動しません。縦向きはボタンが小さくなるため横向きがおすすめです。
表示変更後は公開ページを再読み込みしてください。ローカルだけの変更は公開先に反映されません。
サイズ計算のテスト：`node --test tests/test_viewport.cjs`。
確認済み：ブラウザの390×664（縦）・844×290（横）で全体表示、右端SFXボタン、開始・RETRY確認画面、回転時の状態維持。サイズ関連3件とゲーム関連39件のテスト成功。iPhone実機のSafari・ノッチを含む最終確認は未実施です。

## v7.1：BGMを全面改稿

新曲 **Forest Stroll** は100 BPM・ハ長調・32小節、76.8秒のループです。
旋律とベースを柔らかな三角波に変更し、旋律には長い音と休符を設けました。
主題→変奏→中間部→主題回帰の構成で、ベースも各小節のコードに揃えています。
打楽器は短く小さく、終盤でも音量を上げずリズムの細部だけを追加します。
曲末はGから冒頭Cへ戻り、短い休符で区切りを作ります。ゲーム効果音とルールは変更していません。
音楽データは `music.py` に分離しています。聴感は実際の再生環境でご確認ください。

Pythonと[Pyxel](https://github.com/kitao/pyxel)で作った、反射神経を測る数字タップゲームです。
5行×8列の40マスにランダム配置された数字を探す、横長画面向けのゲームです。

- **数字の範囲** — `1～10`、`1～20`、`1～30`、`1～40`から選べます。
- **ORDER MODE** — 選んだ範囲の数字を1から順番に探します。
- **RANDOM MODE** — 選んだ範囲のお題を重複なしでシャッフルし、表示された順番に探します。

どの範囲でも盤面は5行×8列のままです。例えば`1～10`では、40マスの中に1～10をランダム配置し、残り30マスは押せないロックパネルになります。

## ブラウザで遊ぶ

### v7.0：森のすうじ対決

- 最初に `CPU DUEL`（実際に数字を取り合う対戦）か `SOLO PRACTICE`（従来のタイムアタック）を選択。
- 範囲を選び、対戦では `EASY / NORMAL / HARD` を選びます。その後 `START ORDER / START RANDOM` で開始。
- 対戦は先に見つけた側が1点。全問終了時に60％以上（6・12・18・24点）取れば勝利です。左の黄色い線が目標。左右のゲージが各自の得点です。
- 正解後は0.65秒の結果表示。獲得済みパネルは `YOU / CPU` の色付き裏面になります。
- CPUの探索時間は毎問抽選：EASY 4～6秒、NORMAL 2.4～4.2秒、HARD 1.3～2.6秒。入力位置を読んで正解する仕組みではありません。同一フレームはプレイヤー優先。
- ミスはCPUの残り探索時間を最大0.3秒短縮します。直後の猶予0.25秒を目安とし、既に迫った期限を延ばすことはありません。マスは引き続き選択可能です。
- 確認中はCPU・時計・BGMを停止。キャンセル後も盤面を隠して2秒カウントし、再開します。
- 対戦結果は双方の得点・ミス・平均正解回答時間を表示。最高得点は範囲×ルール×難易度で分離し、この起動中だけ記録します。
- 76.8秒のオリジナルBGMと句境界での編曲切替、CPU先取音・敗北ジングルを搭載。BGMはv7.1で全面改稿しました。原作の音源・画像は含みません。
- 下記の100点制・最速タイムの説明は **SOLO PRACTICE限定** です。対戦は上記ルールを使用します。

最新版はローカルで確認できます。GitHub Pagesへの反映には、この変更のcommit/pushとPagesワークフローの成功が必要です。

公開先: https://dicek9750.github.io/game/ （未反映のローカル変更は公開先には表示されません）

以下は練習モードの遊び方です。

1. `1-10`、`1-20`、`1-30`、`1-40`から数字の範囲を選びます。
2. `START ORDER` または `START RANDOM` を選びます。
3. `3・2・1` のカウントダウン後、数字の表示とタイム計測が同時に始まります。
4. 画面下中央の `FIND THE` に表示された数字を盤面から探します。
5. 正しい数字を押すとパネルが `YOU` と模様でロックされ、次のお題が表示されます。
6. 選んだ範囲の数字をすべて見つけると、タイム・スコア・ミス数・最大連続正解数が表示されます。
7. `PLAY AGAIN` で同じ範囲・ルールを再プレイ、`MODE SELECT` で選択画面へ戻れます。

間違ったマスを押すと、そのマスが爆弾に変わって爆発し、`MISS!` と表示されます。これは短時間の演出だけなので、数字は消えず、同じマスも引き続き選択できます。ゲームは止まらず、連続正解数だけが0に戻ります。PCではマウス、スマートフォンでは横持ちのタッチ操作を前提としています。

## 遊びやすさとスコア

- `RETRY` — 確認後、同じ範囲・ルールを新しい配置でやり直します。
- `TITLE` — 確認後、範囲・ルール選択へ戻ります。
- `BGM ON/OFF` — オリジナルのチップチューンBGMを切り替えます。
- `SFX ON/OFF` — 正解・ミス・クリアなどの効果音を切り替えます。
- 確認画面では盤面を隠し、タイマーも一時停止します。
- 盤面を囲む40個のセグメントと左の縦ゲージで進行が分かります。
- 左の`YOU`がプレイヤー、右の`CPU`がライバルです。正解時はプレイヤーがジャンプして喜び、CPUは悔しがります。ミス時は逆にCPUが喜びます。
- 正解したマスには、発光枠・チェックマーク・火花のエフェクトが表示されます。
- ミスしたマスには、導火線付き爆弾・爆発・煙の連続エフェクトが表示されます。
- 選んだ範囲の25%・50%・75%付近でチェックポイント表示が出ます。50%・75%ではBGMの演奏が一段ずつ厚くなります。
- 正解1個につき100点に加え、連続正解数×10点（最大200点）のボーナスが入ります。ミスによる減点はありません。
- 4つの範囲×2つのルールの8通りごとに、ページを開いている間の最速タイムを記録します。

選択画面のキーボード操作は、`1`～`4`で範囲選択、`O`でORDER開始、`R`でRANDOM開始です。ゲーム中は`R`でリトライ、`T`でタイトル、`B`でBGM切替、`S`で効果音切替、確認画面では`Y` / `N`が使えます。

> 初回表示ではPyxelのWebランタイムを読み込むため、ゲーム開始まで少し時間がかかる場合があります。

## ローカルで起動する

### ブラウザ版（Pythonのインストール不要）

HTMLからPythonファイルを読み込むため、`index.html` を直接ダブルクリックするのではなく、ローカルWebサーバーを使います。

Python 3がインストール済みの場合、リポジトリのフォルダーで次を実行します。

```powershell
python -m http.server 8000 --bind 127.0.0.1
```

その後、ブラウザで http://localhost:8000/ を開いてください。ゲーム本体はブラウザ内で実行されるため、ローカルへのPyxelインストールは不要です。

### デスクトップ版

Python 3.11以降をインストールし、次を実行します。

```powershell
python -m pip install -r requirements.txt
python game.py
```

## GitHub Pagesへの公開

`.github/workflows/pages.yml` が、`main` ブランチへのpush時に自動でGitHub Pagesへ公開します。
初回だけ、GitHubリポジトリの **Settings → Pages → Build and deployment → Source** で **GitHub Actions** を選択してください。

## ファイル構成

- `game.py` — Pyxelで描画・入力・ゲーム進行を行うゲーム本体
- `music.py` — オリジナルBGMの譜面、音色、強弱、編曲
- `game_logic.py` — 配置、正誤判定、一時停止を含むタイム計測を担う独立したロジック
- `index.html` — Pyxel Webを読み込み、PC・スマートフォン向けに表示するページ
- `player.html` — 640×360のゲーム専用ページ（通常はindex.htmlから開きます）
- `viewport.js` — 可視領域に収まる拡大縮小と画面回転への追従
- `requirements.txt` — デスクトップ実行用のPyxelバージョン
- `tests/test_game_logic.py` — ゲーム進行の自動テスト
- `tests/test_battle.py` — CPU先取、24通りの設定、勝利ライン、ミス、一時停止のテスト
- `tests/test_bgm_structure.py` — BGM構造とキャラクター反応のテスト
- `.github/workflows/pages.yml` — GitHub Pagesの自動公開設定

## テスト

v8.0の確認：Python 58件＋画面サイズ関連のNode.js 3件、計61件が成功。CPU探索表示が期限を変えないこと、停止中の時計・探索、キーボード移動と決定、Escの終了キー競合防止、HELP、非表示イベント後の明示的な復帰、停止中のタイトル確認を含みます。ブラウザではHELP、CPU先取・探索枠・獲得履歴、Esc停止と2秒復帰を確認しました。iPhone実機のSafari・アプリ切り替え、音楽の聴感評価は今回未確認です。

```powershell
node --test tests/test_viewport.cjs
```

v7.0の確認：自動テスト36件が成功。ブラウザでCPU自動先取・10問ランダムの敗北結果、タイトル確認と2秒復帰、練習でミスした2番を1番正解後に再選択できること、左右の反応を確認しました。スマートフォン実機のタッチ操作と音楽の聴感評価は未確認です。

ゲームロジックのテストには外部ライブラリは不要です。

```powershell
python -m unittest discover -s tests -v
```

## 技術メモ

- v6.5では数字のコントラストと専用16色パレット、パネルの陰影、キャラクターの滑らかなジャンプを調整しました。
- v7.1のBGMは全演奏レベルで同じ旋律と和声を使用し、リズムだけを変化させます。ループ長は76.8秒です。
- 正解光と爆発の広がりを抑え、同じマスへの新しい入力では古い演出を置き換えます。

- ブラウザ版はPyxel 2.9.8のWebランタイムをCDNから読み込みます。
- BGMと効果音はPyxel内蔵の波形だけで作ったオリジナルです。外部の音声ファイルは使用していません。
- BGMは32ステップ単位の32小節を全パート同じ長さに揃えています。持続音もステップに展開し、休符と強弱でフレーズの区切りを作ります。
- 選んだ範囲の50%・75%到達時の演奏レベル変更は、曲中の現在位置を保ったまま次のフレーズ境界で反映されます。確認画面やBGM切替から戻った場合も、3パートを同じ位置から再開します。
- 2頭身キャラクターもPyxelの円・三角形・長方形だけで描画しており、外部画像は使用していません。
- タイムはフレーム数ではなく単調増加時計で計測するため、描画の遅れが結果に混ざりにくい構成です。
- ゲーム盤は横持ち用の640×360論理解像度で描画し、ブラウザ側で画面に合わせて表示します。
