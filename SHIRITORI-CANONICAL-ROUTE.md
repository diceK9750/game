# 絵しりとり・定型パーフェクトルート（ローカル実装）

基点: `98e4e7d89226a3afd22fc236ad90f0573bb38531` / main。
今回の変更は未コミット。レイアウト・ゲーム時間・音声エンジンは変更しない。

## データと判定

- 66札239読みから、68札248読みへ拡張。
- 既存47札の61読みを自然な表記へ修正。既存読みの発音は維持。
- `とり`、`りんご`、`ねこ`、`すいか`等はひらがな。
- `ゴリラ`、`ラッパ`、`ギター`、`ズッキーニ`等はカタカナ。
- `しょくパン`は日本語部分と外来語部分を分ける。
- `melon` IDの絵は🍉なので、名称はメロンに変えず「すいか」を維持。
- 新札: tomato 🍅（トマト・やさい・たべもの）、
  parsley 🌿（パセリ・ハーブ・やさい・かおりづけ）。外部画像なし。
- appleに「しんしゅうりんご」「すりおろしりんご」を追加。既存別読みを保持。
- 全248読みで漢字・許可外文字0件。同じ札の正規化頭文字重複0件。
- `normalize_reading()`はNFC後にカタカナをひらがなへ変換。
  表示・履歴・発見データには元の読みを保持する。
- 小書きかなは境界判定でのみ大きくする。長音末尾は既存ルールどおり
  直前のかなを使う（ギター→た）。内部長音や小書きを消して別語を同一視しない。
- `seen`は正規化キー。ゴリラ/ごりらを別々に使用できない。
- 辞書も正規化キーを使用。v1保存済みの旧ひらがな読みは発見済みとして
  引き継ぐ。表示は新しい自然な読みのまま。クイズの紛らわしい候補除外も正規化。

## 初期ルートと補充の証明

`PERFECT_CANONICAL_SEGMENT`:
トマト → とり → りんご → ゴリラ → ラッパ → パセリ

`starting_route()`は上の6手を分割不能な区間として扱い、残りを
カタログからランダムに探索する。探索は256ノードで打ち切る。
見つからなければ既存`PERFECT_RING`を改編した認証用の直列経路を使う。
この経路は定型区間を分断する自由な回転を行わない。
最初の24札だけ物理位置をシャッフルし、残りはルート順の山札にする。

開始時に`certify_route()`が全手を実際の`chain_step()`で再生する。
各札がその時点で盤面に存在すること、合法読み、seen、同じ位置への補充、
rescue交換、最終的な盤面と山札の完全消費を検証する。
盤面と山札の単純な集合一致だけでは完走証明としない。

## ヒント・CPU・自由な選択

- `PerfectStep`でidentity・表示reading・key・次のrequiredを保持。
- `initial_perfect_route`は初期証明、`perfect_route`は現在参照する証明。
- `_chain_advice`の完全一致状態に、位置指定の次手と認証済みsuffixを保存。
- ヒントとCPUは共通の`chain_advice()`を使う。
- 状態が変わったらidentityから残存経路を再構成し、位置のシャッフルや
  別読みを許容して末尾まで再検証する。
- appleの「し」「す」から「ご」への再合流も、残りを完走できる場合のみ認証。
- プレイヤーの合法な別手は禁止しない。
- 既存経路を再証明できない場合だけ、従来の4096ノード上限の探索で再計画。
  完走経路を見つけたら、それも認証・suffixキャッシュ化する。
- **探索上限に達した状態は不可能の証明ではない。** `perfect=False,
  exact=False`は「未証明」。任意の逸脱後に存在する全解を必ず発見する
  無制限探索は実装しない。完全探索できた不可能状態とは区別する。

## 検証

`tests/test_shiritori_canonical.py`:

- 12/24/36/48枚 × solo/battle × 200 seed = 1,600初期配置。
- 定型6手の連続性、実補充による完走、表示読み・キー・次requiredの対応。
- 48枚全行程でヒント/CPUが証明を維持（探索関数呼び出し禁止で検査）。
- stock残存時の両apple再合流、物理index変更、seen表記違い、無効suffix。
- 合法な寄り道の実入力から再合流:
  トマト→とり→りゅう→うし→しんしゅうりんご→ゴリラ→ラッパ→パセリ。
  トマト→とり→りす→すりおろしりんご→ゴリラ→ラッパ→パセリ。
- 完走が不可能な小盤面で既存探索が合法な最長候補を返すこと。

既存テストの全ひらがな前提・読み総数・自由回転の前提は新仕様に更新。
既存の補充・制限時間・ミス・一時停止・辞書・CPU・連鎖の検査は維持。

ローカルChromiumでは一人用12枚を実タップで完走:
みつばち→ちょう→うし→しょっき→き→キャロット→
トマト→とり→りんご→ゴリラ→ラッパ→パセリ。
結果12/12、ミス0、辞書へのパセリ登録を確認。
Apple別読みの2経路はPython実ルールの入力シミュレーションで検証。

自動回帰: JavaScript 197/197、Python 216/216、JS構文24ファイル成功。
既存DOM回帰: 552/552、盤面安定48/48、HUD負例検出成功。
既存音声起動A〜D: native 4/4、拒否注入時の無音継続4/4。
Chromium観測レート48000Hz（Safariの値ではない）。
今回のiPhone Safari/LINE/Android実機と音質聴感は未確認。

ローカル計測（PC Python、48枚、100 seed）:
開始中央値1.278ms、最大1.647ms。
証明済み次手4,800回: 中央値0.006ms、最大0.051ms。
この数値はスマホ/WASM性能保証ではない。

実行:
```
node --test tests/*.cjs
.venv\Scripts\python.exe -B -m unittest discover -s tests -p "test_*.py"
git diff --check
```
ローカル配信後、`tests/browser-layout.html`と`tests/audio-startup.html`で
ブラウザ回帰を再実行できる。
