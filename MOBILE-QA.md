# スマートフォン表示修正（2026-09-11）

## 変更範囲

- `mobile-layout.css`: 両ゲーム共通のコンパクト表示。幅700px以下、または高さ650px以下で適用。高さ方向にも装飾・配置を段階調整。PCの通常サイズの既存スタイルは維持。
- `shiritori-ui.js`: 設定・プレイ・閲覧画面を区別する属性を追加。辞書や説明のスクロールは残し、プレイ画面はスクロールさせない。
- `index.html`, `player.html`: dynamic/small viewport fallback、viewport-fit、キャッシュ更新、共通CSS読込。
- `viewport.js`: 既存の visualViewport → iframe 実寸反映を維持。回転中の0/NaNをinnerWidth/innerHeightへフォールバック。
- `.github/workflows/pages.yml`: 新CSSを公開成果物に含める。
- `tests/test_mobile_layout.cjs`: フォールバック・読み込み順・高さ方向の対応を回帰テスト。

Safe Areaは親ページmainが四辺に確保する。iframe内モバイルCSSで再度余白を積まない。
ゲームのPython、ルール、CPU、スコア、音声処理は変更なし。

## 実測した範囲

ローカルHTTPサーバーとCodex内Chromium系ブラウザで、実ゲームを起動。
設定・プレイ・一時停止のボタン位置とclientHeight/scrollHeightをDOM実寸で確認し、画像でも確認した。

| 表示領域（CSS px） | 確認内容 |
|---|---|
| 320×460 | 最小縦画面。両ゲーム設定・プレイ、休憩。絵しりとり設定はキャラクターを省略 |
| 375×559 | 小型iPhoneのバー表示時相当。両ゲームの主要画面 |
| 360×800 | Android縦相当。両ゲーム設定・プレイ |
| 402×750 | 大型iPhoneのバー表示時相当。両ゲーム設定・プレイ |
| 844×390 | 横向き。両ゲームプレイ |
| 844×290 | ブラウザUIで高さが減った横向き。設定・プレイ・休憩 |
| 667×275 | 特に低い横向き。設定・プレイ・休憩 |
| 1280×720 | PC。既存レイアウトとプレイを確認 |

プレイ中の連続リサイズ、縦横回転でもカード位置・獲得状態がリセットされないことを実際の回答後に確認。
親ページを再読込せずにiframeが新しい実寸に追従する。
モーダルはviewport内の最大高さを持ち、必要な場合のみ内部スクロール。

## 操作性の例外

- 主要メニュー・ヒント・一時停止は44pxを維持。
- 数字盤面は指定の5×8を維持。320px幅では8列それぞれの横幅44pxは数学的に確保できない。
- 再修正では盤面の固定最小高さを廃止。ヘッダー・問題・必須操作を除いた残りの高さにカードを配分し、プレイ中のスクロールを禁止。極端に低い画面ではカードの高さは44px未満になる。
- 既存PCの絵しりとり設定は内容量により内部スクロールあり。通常PCのデザインは変更していない。

## 縦スクロール再修正の確認

320×400、375×500、393×600、430×750、600×700、667×250、844×300、844×550で、両ゲームの設定・プレイを再確認。
表示中の画面のclientHeightとscrollHeightが一致し、下端を越える操作ボタンがないことをDOMで測定。
実際のスクロール操作でも位置が0のまま変化しないことを確認した。
320×400の絵しりとり盤面、667×250の対戦設定・対戦盤面・一時停止は画像でも確認。
一時停止は必要に応じてモーダル内部のみスクロール可能で、主要ボタンは表示領域内にある。
設定画面は低い縦画面でラベルを選択肢の横に配置し、低い横画面では難易度を1行にまとめる。
ゲームロジックは変更なし。Node 55件、Python 170件のテストが成功。

## 未検証

### キャラクター領域・連鎖表示の再配置

- `character-layout.css` を最後に読み込み、Pagesにも同梱。既存の全身ポーズアトラスを設定画面・絵しりとり盤面の専用領域に使用。
- 設定は縦持ちで上が操作欄・下が全身キャラクター、横持ちで左が全身キャラクター。320×400では操作を優先してキャラクターを大幅縮小する。
- 絵しりとりは盤面とキャラクターを `.sh-arena` に分離。実際の盤面領域の幅・高さの小さい方から一辺を計算し、全パネルを正方形にする。12枚と24枚表示（36/48枚コースの補充時も共通）に対応。
- 320×400、393×650、360×800、667×250、844×390、1280×720で設定・プレイのスクロール量と操作ボタンを確認。12枚対戦、24枚一人用、48枚一人用を操作して確認。数字設定とプレイも小画面で再確認。
- 数字の3連鎖を実際に発生させ、連鎖表示と盤面の矩形が交差しないことを測定。連鎖表示はゲージ内に限定し、装飾も領域外へ描画させない。
- Nodeテスト59件成功。ゲームルール・CPU・音声は変更していない。新画像生成はせず、既存の全身画像を再利用。

### 画面遷移・結果画面の追加監査

- カウントダウンと結果カードを説明文用のスクロール設定から分離。カウントダウン、通常勝利、パーフェクト勝利、絵しりとりの結果、一時停止、ゲーム選択を実ブラウザで操作して監査した。
- パーフェクト結果は320×400、375×500、393×600、430×750、360×800、667×250、844×300、1280×720で、スクロール可能な要素のはみ出し・画面外ボタン・ボタン同士の重なりがないことを測定。スクロール操作後も結果カードのscrollTopは0。
- カウントダウン・ゲーム選択・絵しりとり一時停止は320×400、667×250、393×600、1280×720で再確認。絵しりとり結果も上記8サイズで確認。
- 数字の結果は低い横画面で2列、縦画面ではコンパクトな1列。高さ850px以下のPC窓にも適用し、パーフェクトボーナス追加時のはみ出しを防ぐ。
- 辞書・説明文・対戦履歴は長い内容の閲覧にスクロールが必要なため残す。説明と振り返りは本文だけをスクロールさせ、戻るボタンを分離した。
- ゲームロジックは変更なし。Node 56件、Python 170件成功。Safari/LINEの実機検証・本番公開は未実施。

**iOS Safari・Android Chrome・LINE内ブラウザの実機検証は未実施。**
実機/リモート端末が接続されていないため、ブラウザバーの実挙動やDynamic Islandを含めて検証済みとはしない。
本番公開後、各実機で縦横回転、バーの開閉、ノッチ/ホーム領域、文字拡大を確認する必要がある。
この変更ではGitHub Pagesへのpush/deployは実行していない。
